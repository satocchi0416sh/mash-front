import { useHistory } from "react-router-dom"

function Top(props) {
    const { id } = props
    const history = useHistory()

    return (
        <div className="top-page">
            
        </div >
    )
}
export default Top